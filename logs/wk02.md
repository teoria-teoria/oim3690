# week 02

**date:** may 25, 2026

## what i worked on

- built hello.html. the hand-written skeleton first, then an ai version next to it in the same file so i could compare.
- built about-me.html with claude code. semantic html, covers clave, churromania, letallgirls, stuvi.
- long memorial day weekend so most of this happened in one sitting.

## what we learned in class

session 03 was monday 5/25. covered html structure and what actually goes in the `<head>`. then semantic html: using `header`, `nav`, `main`, and `footer` instead of stacking divs. also accessibility basics, alt text on images, the `lang` attribute, keeping headings in order. and the read-find-ask-verify workflow for reading ai code instead of just trusting it.

the semantic layout that finally clicked for me:

```html
<header>
  <nav>...</nav>
</header>
<main>
  <section>...</section>
</main>
<footer>...</footer>
```

## what i'm still confused about

- when do i use `<article>` vs `<section>`. still fuzzy.
- accessibility. i get alt text but not sure how far you're supposed to go for a class project.

## ai usage

prompted claude code: "write about-me.html using semantic html. cover clave, churromania, letallgirls, stuvi. lowercase first person, short sentences."

claude gave me a full file with header, main, multiple sections, and footer. i kept most of it. changed two paragraphs that sounded too formal. removed a line that said "i am passionate about" because i'd never say that.

tried the read-find-ask-verify thing from class instead of just pasting it in. read the file top to bottom, found the tags i didn't recognize, asked claude what `<section>` was doing vs a plain div, then checked it rendered right in the browser. caught the formal paragraphs that way.

## mp1

starting to think about who to build a site for. no client locked in yet. probably someone with a real business so the content isn't fake.
